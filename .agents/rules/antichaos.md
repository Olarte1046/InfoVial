---
trigger: always_on
---

Never hallucinate files, APIs, routes or components.

Always inspect current implementation before editing.

Never rewrite unrelated sections.

Preserve existing working logic unless explicitly requested.

Prefer incremental edits over refactors.

Do not invent imports.

Do not assume backend endpoints exist.

Before making changes:
- analyze architecture impact
- identify dependencies
- explain breaking risks